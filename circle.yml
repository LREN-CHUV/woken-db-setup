machine:
  pre:
    - curl -sSL https://s3.amazonaws.com/circle-downloads/install-circleci-docker.sh | bash -s -- 1.10.0
  services:
    - docker

dependencies:
  override:
    - docker version
    - docker info
    - docker build --rm=false -t lren/flyway:4.1.2 .

test:
  override:
    - docker run -i -t lren/flyway:4.1.2 "" || true

#deployment:
#  hub:
#    branch: master
#    commands:
#      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
#      - docker push lren/flyway:4.1.2
